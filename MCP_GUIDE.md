# 🤖 MCP 使用指南（客户版）

> 让 Chatbox（或任何支持 MCP 的 AI 客户端）直接用**自然语言**调用你的小红书私信发送系统。
> 无需打开控制面板，在聊天框里说一句话就能发私信、查状态、加接收方。

---

## 一、前提条件

| 条件 | 说明 |
|---|---|
| ✅ 已部署本系统 | `docker compose up -d` 启动成功，`xhs_app` 和 `xhs_mongo` 均为 Up |
| ✅ Chatbox 版本 | **1.14 或更高**（支持远程 MCP 服务器） |
| ✅ 端口放行 | 服务器防火墙/安全组需放行 **MCP 端口（默认 8090）** |

> 不同云厂商放行端口位置：阿里云「安全组」、腾讯云「防火墙」、AWS「Security Group」、华为云「安全组」。
> 放行规则：`入方向` → `TCP` → `8090` → `来源 0.0.0.0/0`（或限定你常用的 IP）。

---

## 二、添加 MCP 服务器（两步）

### 第一步：获取 MCP 地址

```
http://<你的服务器IP>:8090/mcp
```

> 若你修改过 `.env` 的 `MCP_PORT`，把 8090 换成你的端口。
> 验证方式：浏览器打开 `http://<服务器IP>:8090/mcp`，能看到提示（非 404）即服务正常。

### 第二步：Chatbox 中添加

1. 打开 Chatbox → 右下角 **设置**
2. 找到 **MCP** → **添加服务器**
3. 类型选择：**远程（HTTP）**
4. 服务器名称：`xhs-sender`（随意）
5. URL 填入上面的地址
6. 点击 **测试连接** → 显示成功 → **保存**

> 💡 如果服务商提供了一键安装链接（`chatbox://mcp/install?server=...`），直接点击链接即可自动添加，无需手动配置。

---

## 三、首次使用前的准备（重要）

MCP 工具依赖你 MongoDB 里的数据，首次使用前请先在**控制面板**（`http://<服务器IP>:8080`）完成：

1. **添加发送设备** → 设备管理页（至少 1 台）
2. **添加检测号设备** → 设备管理页（必须与 `.env` 中 `CHECK_USER_ID` 对应，用于发送前健康检查）
3. **添加接收方** → UserId 管理页（或在 Chatbox 里用 `xhs_add_receiver` 工具添加）
4. **添加文案** → 文字消息页（至少 1 条，发送时随机使用）

---

## 四、开始使用（对话示例）

添加完成后，直接在 Chatbox 对话窗口说：

| 你想做什么 | 对 Chatbox 说 |
|---|---|
| 发一条私信 | “帮我给下一个用户发一条私信” |
| 查看服务端状态 | “查看一下我的服务端连接状态和 key 是否有效” |
| 查看设备列表 | “列出我所有的发送设备” |
| 添加接收方 | “把 6931587600000000xxxxx 添加到待发送队列” |
| 发多条 | “今天还有 50 个用户没发，帮我逐个发送” |

Chatbox 会自动调用对应工具并返回结果，例如：

```
✅ 任务执行完成（发送设备 1234567890）
   - 接收方: 6931587600000000xxxxx
   - 状态: 成功
```

---

## 五、可用工具一览

| 工具名 | 作用 | 说明 |
|---|---|---|
| `xhs_send_next` | 自动发送一条私信 | 领取可用设备 → 领取接收方 → 健康检查 → 发送 |
| `xhs_list_devices` | 列出全部发送设备 | 昵称 / userid / 备注 |
| `xhs_server_status` | 查看服务端与 Key 状态 | 在线 / 离线 / Key 无效 / 未配置 |
| `xhs_add_receiver` | 添加接收方到队列 | 自动去重，已在队列则提示 |

---

## 六、常见问题

### 1. 测试连接失败？
- 检查端口是否放行（防火墙/安全组）
- 检查 `docker compose ps` 是否两个容器都 Up
- 检查 URL 是否完整（`http://IP:8090/mcp`，不要漏 `/mcp`）

### 2. 提示"未找到检测号"？
- 控制面板设备管理中确认已添加检测号设备
- 确认 `.env` 的 `CHECK_USER_ID` 与该设备 userid 一致
- 改完 `.env` 需 `docker compose up -d` 重启

### 3. 提示"没有可用的发送设备"？
- 设备可能都在冷却期内（防止频繁发送触发风控）
- 等待冷却结束，或在控制面板检查设备健康状态

### 4. 提示 Key 无效/过期？
- 检查 `.env` 的 `PROXY_API_KEY` 是否正确
- Key 过期请联系服务商续费（接收方数据不会丢失，会自动退回队列）

### 5. 手机/其他设备能用吗？
- 可以。MCP 服务器在你的服务器上运行，**任何设备上的 Chatbox**（桌面/Web/手机）只要添加同一个 MCP 地址即可使用。

---

## 七、安全提醒

- MCP 端口默认**无鉴权**（依赖部署环境的网络隔离），强烈建议：
  - 用防火墙/安全组把 8090 端口**限制为仅自己的 IP 可访问**，或
  - 通过 VPN / 内网访问
- 不要把 `PROXY_API_KEY`、MongoDB 密码泄露给他人
- 本工具仅限合法合规场景使用
